# Database Configuration
DB_CONNECTION="Server=localhost\SQLEXPRESS;Database=MathRacerDb;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True;"

# Firebase Configuration
# IMPORTANTE: En desarrollo local, usa el archivo firebase-credentials.json en la raíz del proyecto
# En producción (Render/Heroku/etc), configura esta variable con el contenido JSON completo:
# FIREBASE_CREDENTIALS_JSON='{"type":"service_account","project_id":"...","private_key":"...",...}'

